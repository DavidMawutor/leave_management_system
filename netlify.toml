[build]
  command = "ng build --configuration production"
  publish = "dist/leave-management-app/browser"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
